<%# < style="color: green"><%= notice %>


<div id="fmodels">
  <table>
    <tr>
      <th>Title:</th>
      <th>Created:</th>
      <th>Updated:</th>
      <th></th>
      <th></th>
    </tr>
    <% @fmodels.each do |fmodel| %>
      <tr>
        <td><%=fmodel.title %></td>
        <td><%=fmodel.created_at.strftime('%m/%d/%Y') %></td>
        <td><%=fmodel.updated_at.strftime('%m/%d/%Y') %></td>
        <td><%= link_to "Show", fmodel %></td>
        <td><%= link_to "Edit", edit_fmodel_path(fmodel) %></td>
      </tr>
    <% end %>
  </table>
</div>


<%= link_to "New fmodel", new_fmodel_path %>
